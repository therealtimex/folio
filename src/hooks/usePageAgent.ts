/**
 * Dummy hook for Page Agent to avoid breaking UI components
 */
export function usePageAgent(config: any) {
    // This will be implemented when Agent support is added to Folio
    // console.log('[usePageAgent] Stub:', config.page_id);
    return null;
}
